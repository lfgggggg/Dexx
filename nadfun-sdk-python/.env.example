# Monad Testnet Configuration
# RPC endpoint for HTTP connections
RPC_URL=

# WebSocket endpoint for real-time event streaming
WS_URL=

# Your wallet private key (without 0x prefix)
# NEVER commit your actual private key to version control
PRIVATE_KEY=your_private_key_here

# Token address for trading
TOKEN=0x...

# Multiple token addresses for DEX monitoring (comma-separated)
TOKENS=0x...

# Trading parameters
AMOUNT=                    # Amount in MON (e.g., 0.1)
SLIPPAGE=                  # Slippage percentage (e.g., 5)